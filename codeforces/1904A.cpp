#include <bits/stdc++.h>
using namespace std;
int main(){
	int t;
	cin >> t;
	while(t--){
		int a,b,x1,y1,x2,y2;
		cin >> a >> b;
		cin >> x1 >> y1;
		cin >> x2 >> y2;
		int ans = 0;
		set<pair<int,int>> st1,st2;
		for(int i = 0 ; i < 8 ; i++){
			st1.insert({x1+,})
		}
	}
	return 0;
}
